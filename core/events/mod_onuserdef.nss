///////////////////////////////////////////////////////////////////////////////
// mod_onuserdef
// written by: eyesolated
// written at: March 31, 2015
//
// Notes: This script handles all UserDefined Events of the Module
void main()
{
    /*
    object oMod = GetModule();
    int iEvent = GetUserDefinedEventNumber();
    switch (iEvent)
    {
        default:
            return;
            break;
    }
    SignalEvent(oMod, EventUserDefined(iEvent + 1));*/
}
